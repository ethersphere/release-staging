smoke:
  schedule: "*/2 * * * *"
  image:
    repository: ethdevops/swarm
    tag: janos
  config:
    name: "upload-sync-1M-30"
    swarmReplicas: 100
    target: "swarm-private"
    operation: "upload_and_sync"
    extraFlags:
      - --single
      - --filesize=1000
      - --sync-delay=30

