smoke:
  schedule: "*/5 * * * *"
  config:
    name: "upload-sync-110-5-30"
    swarmReplicas: 5
    target: "swarm"
    operation: "upload_and_sync"
    influxdbEndpoint: "http://swarm-staging-influxdb:8086"
    extraFlags:
      - --single
      - --filesize=110
      - --sync-delay=30

