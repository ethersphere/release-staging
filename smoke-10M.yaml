smoke:
  schedule: "*/5 * * * *"
  image:
    repository: ethdevops/swarm
    tag: anton
  config:
    name: "upload-sync-10M-30"
    swarmReplicas: 20
    target: "swarm"
    operation: "upload_and_sync"
    extraFlags:
      - --single
      - --filesize=10000
      - --sync-delay=30

