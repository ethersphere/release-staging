smoke:
  schedule: "*/3 * * * *"
  image:
    repository: ethdevops/swarm
    tag: anton
  config:
    name: "upload-sync-100M-30"
    swarmReplicas: 20
    target: "swarm-private"
    operation: "upload_and_sync"
    extraFlags:
      - --single
      - --filesize=100000
      - --sync-delay=30

