smoke:
  schedule: "*/8 * * * *"
  image:
    repository: ethdevops/swarm
    tag: anton
  config:
    name: "upload-sync-40M-40"
    swarmReplicas: 100
    target: "swarm-private"
    operation: "upload_and_sync"
    extraFlags:
      - --single
      - --filesize=40000
      - --sync-delay=40
      - --timeout=300
