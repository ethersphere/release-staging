smoke:
  schedule: "*/5 * * * *"
  config:
    name: "upload-sync-10M-90"
    swarmReplicas: 20
    target: "swarm"
    operation: "upload_and_sync"
    extraFlags:
      - --single
      - --filesize=10000
      - --sync-delay=90

