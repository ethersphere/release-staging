smoke:
  schedule: "*/3 * * * *"
  image:
    repository: ethdevops/swarm
    tag: anton
  config:
    name: "upload-sync-50M-30"
    swarmReplicas: 200
    target: "swarm-private"
    operation: "upload_and_sync"
    extraFlags:
      - --single
      - --filesize=50000
      - --sync-delay=30

